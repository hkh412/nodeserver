#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gen-uuid/bin/gen-uuid" "$@"
  ret=$?
else 
  node  "$basedir/../gen-uuid/bin/gen-uuid" "$@"
  ret=$?
fi
exit $ret
